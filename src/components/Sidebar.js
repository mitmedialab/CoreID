var React = require('react');

var Sidebar = React.createClass({

	render(){

		return (
			<div id="sidebar" class="sidebar app-aside">Prelim Sidebar Section</div>
		);
	}

});

module.exports = Sidebar;
